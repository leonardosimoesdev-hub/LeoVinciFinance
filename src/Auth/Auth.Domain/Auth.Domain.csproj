<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Auth.Domain</RootNamespace>
  </PropertyGroup>

  <!--
    Regra de dependência (Especificação Mestre, seção 4):
    Domain NÃO pode depender de EF Core, ASP.NET, JWT, etc.
    Único projeto referenciado é BuildingBlocks.Common (POCO puro, sem infraestrutura).
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\BuildingBlocks\BuildingBlocks.Common\BuildingBlocks.Common.csproj" />
  </ItemGroup>

</Project>
